name: Daily refresh r/longevity evidence

on:
  schedule:
    - cron: '0 0 * * *'  # Daily at midnight UTC
  workflow_dispatch:  # Allow manual trigger

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Install and start Ollama
        run: |
          curl -fsSL https://ollama.com/install.sh | sh
          ollama serve &
          sleep 5
          ollama pull llama3.2:3b
      
      - name: Generate demo data (no Reddit API needed)
        run: python src/generate_demo_data.py
      
      - name: Extract claims from demo data
        run: python src/02_extract_claims.py
      
      - name: Check evidence against PubMed
        run: python src/03_evidence_check.py
      
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: evidence-results
          path: data/processed/*.csv
